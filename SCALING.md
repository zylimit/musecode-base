# 100W 规模指南（SCALING.md）

> 适用：本仓（agentic CLI 基座）从“单人单仓”扩展到 100W 量级任务规模。本指南定义基线、容量模型与扩容 checklist；具体压测数据落地后回填 §6。

## 1. 规模定义（先对齐口径）

“100W”在本仓统一解读为以下基线（取交集，不单看其一）：

| 维度 | 100W 基线 | 说明 |
|---|---|---|
| 任务执行 | 100 万次工具调用/月（≈ 0.4 次/s 均值，峰值 100 并发） | 工具路由（H3）为核心瓶颈 |
| 会话 | 10 万活跃会话，单会话 ≤ 100MB jsonl | 会话与日志（H1）定存储 |
| 扫描 | 单次全仓扫描 ≤ 100 万文件，P95 ≤ 5 分钟 | 检索（H3）定吞吐下限 |

> 非目标：本指南不承诺单机支撑 100 并发模型推理；模型侧容量由上游服务保证，本仓只保证调度与降级正确。

## 2. 容量模型（回填表）

| 资源 | 单任务消耗（估算） | 100W/月总需求 | 瓶颈信号 |
|---|---|---|---|
| CPU | 工具调度 ~50ms CPU/次 | 均值可忽略，峰值按 100 并发 × 单任务 CPU | P95 调度延迟 > 1s |
| 内存 | 单会话常驻 ~10MB + 单任务 ~5MB | 峰值 100 并发 ≈ 1GB + 会话缓存 | OOM / swap |
| 磁盘 | 单会话 jsonl ≤ 100MB，TTL 30 天 | 10 万会话 × 均值 10MB ≈ 1TB（含 TTL 前） | 磁盘 > 70% |
| 子进程 | 单任务 ≤ 2 子进程 | 峰值 ≤ 200 进程 | fork 失败 / zombie 堆积 |
| 外部 API | 单任务 ≤ 3 次调用 | 峰值 300 QPS（含重试） | 429/超时突增 |

落地要求：首次压测后把“估算”替换为实测 P50/P95，并写 ADR 固化。

## 3. 架构策略（按优先级）

1. **有界并发**：全局信号量（默认 32，可配置），超限排队 + 明确排队日志；禁止无界 `spawn`/`Promise.all` 全量。
2. **超时 + 熔断**：外部调用超时 ≤ 30s；连续失败 N 次打开熔断，走 `src/fallback/` 降级分支（H7）。
3. **分片与流式**：文件扫描按目录分片、流式产出；禁止“全读入内存再处理”。
4. **缓存**：仓结构/索引缓存（TTL 可配）；缓存 key 含仓版本，失效可解释。
5. **幂等与去重**：任务 ID 幂等；重试不产生重复副作用（见 QUALITY_CHECKLIST RES-5）。
6. **无状态调度**：调度器本身无状态，状态落会话存储；支持水平复制调度器。

## 4. SLO（100W 下的承诺）

| 指标 | 目标 | 测量 |
|---|---|---|
| 调度可用性 | 99.9%/月（熔断打开不计为不可用，计为降级） | 成功+显式降级 / 总请求 |
| 工具 P95 延迟（不含模型） | ≤ 1s | 调度器埋点 |
| 全仓扫描 P95（100 万文件） | ≤ 5min | 压测基线 |
| 数据丢失 | 0（会话 jsonl fsync，写失败即报错不静默） | 混沌测试 |

## 5. 压测方法（最小可跑）

```bash
# 5.1 基线：单任务延迟（N=100，取 P50/P95）
for i in $(seq 1 100); do /usr/bin/time -f "%e" bash scripts/smoke.sh >/dev/null; done

# 5.2 并发：32 并发 × 100 轮（观察失败率与 P95）
seq 1 100 | xargs -P 32 -I{} bash scripts/smoke.sh >/dev/null

# 5.3 扫描：合成 10 万文件目录，测分片扫描吞吐（按实际扫描器替换命令）
mkdir -p /tmp/scale_probe && seq 1 100000 | xargs -P 8 -I{} touch /tmp/scale_probe/f_{}
# 后续工作（已记 progress.md 待办）：接入真实扫描器后替换下一行
find /tmp/scale_probe -type f | wc -l
```

> 压测前先跑 `bash scripts/smoke.sh` 确认基线绿；压测产物放 `/tmp`，不进仓。

## 6. 观测指标（必埋点）

- `tool_calls_total{tool,status}`、`tool_latency_p95`、`queue_depth`、`breaker_state`。
- `session_bytes`、`disk_usage_ratio`、`subprocess_count`。
- 告警线：P95 > 1s 持续 5min、熔断打开、磁盘 > 70%、429 率 > 1%。

## 7. 降级矩阵

| 故障 | 降级行为 | 用户可见 |
|---|---|---|
| 模型/API 超时 | 熔断 + 返回缓存/骨架结果 | 明确“降级：原因 + 重试建议” |
| 磁盘紧张 | 拒绝新会话 + TTL 加速清理 | “存储不足，已拒绝，清理指引” |
| 并发超限 | 排队 + 排队位通告 | “排队中：n/m，预计等待” |
| 扫描器过载 | 分片缩小 + 限流 | “扫描限流：进度 + ETA” |

红线：任何降级都必须显式表达，禁止空/零值伪装健康（`ARCHITECTURE.md` 红线 4）。

## 8. 扩容 Checklist

- [ ] 并发上限、超时、重试次数全部可配置（有默认值 + 文档）。
- [ ] 会话存储 TTL + 清理 cron 生效，磁盘水位 < 70%。
- [ ] 压测 §5 三项跑过，数据回填 §2。
- [ ] 告警 §6 接通，熔断演练做过一次。
- [ ] 降级矩阵逐行有测试（单测或混沌用例）。
- [ ] 瓶颈 ADR 已写（含“为何不是更大并发/更长超时”的拒绝理由）。
