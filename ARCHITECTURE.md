# ARCHITECTURE.md — 架构看护

## 1. 架构原则（红线）

1. **事实源分层**：现有代码/类型/测试证明**当前行为**；已确认需求（Spec/Brief/用户纠正）决定**目标行为**；兼容承诺决定**迁移约束**。三者冲突时目标行为赢，旧实现与旧测试跟着改，不得用"仓内现状"压过已确认的新规则。
2. **最小改动**：修根因，不修症状；不引入未被要求的接口、文件、依赖。
3. **同步测试**：行为变更必须有测试锁定；测试失败是需求，不是噪音。
4. **显式降级**：外部依赖不可达时，交付物必须显式表达降级态，禁止把空/零值伪装成健康。
5. **可回滚**：生成文件由其 sanctioned writer 写；手写复制冒充产物一律禁止。

## 2. 模块边界

| 模块 | 职责 | 禁止 |
|---|---|---|
| `examples/` | 随仓实例工具（真实小产品，App 自带测试） | 往脚手架脚本里塞产品逻辑 |
| `tests/` | 行为锁定 | 反向依赖 `scripts/` 产物 |
| `scripts/` | 门禁与运维 | 承载产品逻辑 |
| `docs/adr/` | 决策记录 | 存放需求正文（需求放 `docs/REQ-*.md`） |
| `.agents/skills/` | 可复用技能（官方 Project skills 路径） | 放密钥与大二进制 |
| `.agents/memory/` | 项目记忆（官方 Project memory 路径） | 记临时进度/猜测/密钥 |
| `.agents/workflows/` | 可复用 workflow 脚本 | 放密钥；终值非 JSON |
| `.muse/hooks.json` | 项目 hooks 声明（官方路径，跑在沙箱外） | 放未读过的命令 |

## 3. 数据与错误契约

- 错误：沿用同域已有错误类型与包装方式；不新造异常体系。
- 返回：沿用 sibling 代码的形状（key、构造器、默认值）；新功能无 sibling 可参照时才按仓惯例设计。
- 边界：成对边界（start/end、takeoff/landing）用同一约定，并在 ADR/注释中写明 inclusive/exclusive 依据。

## 4. 变更门禁

- 触及公共 API / 数据形状 / 门禁脚本 → 必须写 ADR。
- 新增依赖 → ADR 中说明替代项与拒绝理由。
- 删除能力 → 同步删除消费该能力的 sibling 逻辑，不找替代源续命。

## 5. ADR 流程

1. 从 `docs/ADR_TEMPLATE.md` 复制到 `docs/adr/NNNN-<slug>.md`。
2. 状态流转：`proposed → accepted → superseded`，只追加不改历史。
3. 引用：在 PR/交付说明中贴 ADR 编号。

## 6. 例外

- 违反任一红线需在交付中单列“架构例外”：原因、影响面、回补计划。无例外声明的红线违反视为未完成。
