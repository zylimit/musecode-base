# Design-Brief 模板（行为脊柱）

> 来源：cc `design-brief-builder/templates/design-brief-template.md`（适配本仓路径）。
> 复制为 `docs/DESIGN-BRIEF-<slug>.md` 后填写。与 `DESIGN-*.md`（视觉身份）成对，行为改本文件，视觉改 DESIGN。

# Design Brief — [项目名]

> 输入：`docs/REQ-<slug>.md` §使用情境/关键流程。输出：给 design-maker 的行为输入。

## 形态与使用情境

- 形态：[界面型 / 终端型 / 对话式 Agent / 嵌入式，可叠加]
- 使用情境：[站/坐、单手/双手、几秒/几小时、打断频率、设备光线]
- 首屏最要紧的一个动作：

## 信息架构

- 导航结构：
- SCREEN-01 [页面名]：[职责一句话]
- ……
- 每页落点检查：每条 P0 有承接页，每页有流程落点（无落点页问用户去留）。

## 页面与状态（每页八态：空/加载/成功/错误/冲突/离线/无权限/Agent 工作中）

### SCREEN-01 [页面名]

- 布局与组件（CMP 编号）：
- **必需状态**：[八态中本页必须显式设计的：空/加载/成功/错误/冲突/离线/无权限/Agent 工作中]
- **响应式**：[断点行为：窄屏/宽屏/超宽屏]
- 交互备注：

## 交互与流程

- 关键流程（含异常分支）：
- 授权点（不可逆操作在哪里停下来等人）：
- AI 行为（如有）：运行态/透明度/可干预点（见 skill `agent-ux-patterns` 引用）：

## 组件清单

| 编号 | 组件 | 用在哪几页 | 备注 |
|---|---|---|---|
| CMP-01 | | | |

## 假设与待确认

- [假设] ……（待用户确认）
- 冲突取舍记录（新方向与已定项冲突时的选择）：

## 交接

- 页面/组件数 → dev-planner 工作量输入。
- 形态与参照决策 → `progress.md` Decisions。
