# 需求模板（REQ）

> 复制为 `docs/REQ-<slug>.md` 后填写。每个需求必须可验收、可回归。

## 1. 背景与目标

- 背景：
- 目标（一句话）：
- 非目标：

## 2. 范围

- 在内：
- 在外：
- 影响模块（`src/` 路径）：

## 3. 功能需求

| 编号 | 描述 | 优先级 |
|---|---|---|
| FR-1 | | P0/P1/P2 |

## 4. 错误 / 边界 / 负例

| 编号 | 场景 | 期望行为 |
|---|---|---|
| ER-1 | 输入缺失/非法 | |
| ER-2 | 外部依赖不可达 | 显式降级（见 ARCHITECTURE.md 红线 4） |
| ER-3 | 冲突/重复 | |

## 5. 验收标准（必须可执行）

- [ ] AC-1：命令/操作 → 期望输出（含退出码）。
- [ ] AC-2：测试文件 `tests/...` 通过。
- [ ] AC-3：门禁脚本通过。

## 6. 约束

- 架构红线：无例外 / 有例外（见 ADR-?）。
- 性能/兼容要求：

## 7. 回归

- 既有测试影响面：
- 需新增测试：
