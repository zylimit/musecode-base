# 架构设计模板（ARCH-DESIGN）

> 来源：cc `arch-designer/templates/architecture-design-template.md` + codex 同名模板（合并，路径适配本仓）。
> 复制为 `docs/ARCH-DESIGN-<slug>.md` 后填写。S 级小改不用本模板，直接写代码。

<!-- S/M/L：S=单文件实现细节（不用本模板）/ M=模块内多文件 / L=跨模块·公共契约·数据归属（必须 ADR） -->

## 0. 架构目标与约束

- 目标：
- 约束（技术/组织/合规）：
- 关键驱动因素（不变量/质量目标/现实约束/关键未知，3–7 条）：

## 1. 架构概览（C4 二层：Context + Container，文字版即可）

-

## 2. 业务追演与事实归属

> 核心事件追演（输入→参与者→事实写入→读出→异常），每演一次记一次，作为"事件检验边界"证据。

### 事实归属表

| 数据 | 唯一真相源 | 其余是缓存还是投影 | 同步/失效规则 |
|---|---|---|---|
| | | | |

## 3. 分层与模块

### 分层（只许同层或向内）

| 层 | 职责 | 允许依赖 |
|---|---|---|
| | | |

### 模块清单

| 模块 | 路径 | 职责 | 依赖 | 禁止依赖 |
|---|---|---|---|---|
| | | | | |

（与 `.agents/harness/module-catalog.json` 同步，`arch-check.sh` 校验。）

## 4. 公共契约

| 契约 | 类型（API/事件/schema/文件格式） | 生产者 | 消费者 | 版本策略 |
|---|---|---|---|---|
| | | | | |

## 5. 变化轴与扩展点（开闭原则）

| 变化轴 | 扩展点 | 不许碰 |
|---|---|---|
| | | |

## 6. 质量场景与方案比较

> 先写可区分方案的质量场景（触发/刺激/环境/对象/期望响应/判据），再让候选同场景同断点比较。

| 场景 | 方案 A | 方案 B | 结论 |
|---|---|---|---|
| | | | |

- 敏感假设：
- 跨属性代价（重试↔流量放大、缓存↔越权可见等）：
- 推翻条件（什么证据出现则重选）：
- 最小验证（ spike / 压测 / 原型）：

## 7. 包络（成功 + 异常 + 回滚）

- 成功路径：
- 异常（超时/冲突/降级/外部失败）：触发 / 行为 / 数据保护 / 下一步：
- 回滚/补偿：

## 8. 关键决策（ADR 索引）

| ADR | 标题 | 状态 | Enforced-by |
|---|---|---|---|
| `docs/adr/NNNN-*.md` | | proposed/accepted | |

## 9. 架构验收

- 设计评价：场景是否覆盖驱动因素；最小反证验证是否执行。
- 工程约束检查：分层/禁边/catalog 同步（`arch-check.sh` 输出）。

## 10. 债务与还债计划（棕地项目）

| 债务 | 位置 | 影响 | 还债计划 |
|---|---|---|---|
| | | | |

## 11. 未决事项与下游影响

| # | 事项 | 影响 | 押后到 |
|---|---|---|---|
| | | | |
