> 来源：cc-base/.claude/skills/dfx-designer/examples/after-sales-dispatch-dfx.md（整件收录，路径适配见本仓同名 skill）

# DFX Spec — 售后工单派单

> 生成：2026-09-10 · 模式：设计 Design-in · 档位：M · 状态：待批准
> 档位定义与判定规则见 `quality-attributes.md`（随 setup --with-harness 装进 .claude/rules/，源在 .claude/harness/ext/rules/）；本项目无 module-catalog，档位只进本文档，由 code-review 与 tester 守。
> 范例与 product-spec-builder/examples/after-sales-dispatch.md、arch-designer/examples/after-sales-dispatch-arch.md 配套。

## 1. 优先级栈（冲突时前压后）

1. 可靠性——单子不丢（SC-2 十分钟内到手，丢一单就是派错一单）
2. 隐私——客户电话只客服可见（个保法 + Q-2）
3. 可服务性——三人班组没有运维，出事要能自己看出来
4. 成本——一台内网机，不加机器
5. 性能——20 人在线，5 秒内可见即可

理由：用户原话「只能保一个：派单准」；内网单机、无运维，任何要加机器或加人的方案排在后面。

## 2. 维度总表（十三维）

| # | 维度 | 25010 | 适用 | 全局档 | 场景（六要素摘要；S 档一行「刺激 → 响应」） | 度量（数字+单位+测法） | 设计对策 | 验证落点 |
|---|---|---|---|---|---|---|---|---|
| 1 | 可靠性 Reliability | 可靠性·无故障 | ✅ | high | 手机断网时客服重试提交 → 只建一单 | 重复建单 0 单 / 月（周报核对）；提交失败静默吞掉 0 次 | 幂等键唯一约束（ADR-2）、不吞错 | 回归测试「同幂等键重放只建一单」+ fitness no-silent-failure |
| 2 | 韧性 Resilience | 可靠性·容错 / 可恢复 | ✅ | medium | 进程崩溃 → 容器自动拉起 | MTTR ≤ 30 分钟（人工重启计时） | docker restart=always；重试只在 DB 层（§3 预算表）；无多副本 | 拔掉容器实测一次 |
| 3 | 安全 Security | 安全性 | ✅ | high | 维修工用自己的会话请求他人工单 → 拒绝 | 越权读取 0 次（专项测试）；高危漏洞 0 | 入口统一鉴权、按工单归属判权（§5 威胁表） | 权限专项测试 + npm audit |
| 4 | 功能安全 Safety | 功能安全 | N/A：纯信息系统，不控制设备 | minimal | — | N/A | — | — |
| 5 | 隐私 Privacy | — | ✅ | high | 维修工端拉工单 → 响应不含电话字段 | 非客服角色 API 返回电话字段 0 次；日志含电话 0 行 | `auth.can(role, 'customer.phone')` 一处判断；日志脱敏 | fitness no-pii-in-logs + 角色矩阵测试 |
| 6 | 性能 Performance | 性能效率 | ✅ | medium | 20 人同时在线录单看单 | 录单提交到组长列表可见 ≤ 5 秒 P95；20 并发错误率 0（20 人 × 每人 30 单/日 ≈ 均值 0.007 QPS，峰值 ×10 也不到 0.1 QPS，算过不是拍的） | 无缓存层；列表分页 50 条；延迟按 §3 分到三层 | 20 并发脚本压一次 |
| 7 | 可服务性 Serviceability | 可靠性·可用性 | ✅ | medium | 客服说「单子没到」→ 老周自己能查 | 故障定位 ≤ 15 分钟；health 端点可用率 100% | JSON 日志带工单 id；`/api/health` | health 端点探针 + 演练一次 |
| 8 | 可安装性 Installability | 灵活性·可安装 | ✅ | medium | 换一台内网机重装 | 干净机安装 ≤ 5 步、≤ 30 分钟；回滚 = 换镜像 tag ≤ 5 分钟 | compose 一键起；配置只读环境变量 | 干净虚拟机装一次计时 |
| 9 | 可测试性 Testability | 可维护性·可测试 | ✅ | medium | 新加派单规则不起数据库也能测 | 派单规则纯函数单测覆盖率 100%；全量测试 ≤ 2 分钟 | 规则纯函数、仓储接口可替换 | test-builder 基建 + 覆盖率报告 |
| 10 | 可修改性/扩展性 | 可维护性·可修改 / 灵活性·可伸缩 | ✅ | medium | Q-1 加「24 小时未接改派」规则 | 新增一条派单规则改动 ≤ 2 文件 | 策略注册表（ADR-3） | code-review Stage 2 核 |
| 11 | 归一化 Normalization | 可维护性·可复用 | ✅ | low | 桌面与手机页各写一套表单 | 表单与列表组件各 1 套 | 组件按 DESIGN.md token 共用 | code-review 归一 lens |
| 12 | 成本 Cost | — | ✅ | medium | 不加机器不加人 | 内网机 1 台；CI 全量 ≤ 5 分钟 | 单机单容器 | CI 时长趋势 |
| 13 | 能效 Energy efficiency | 性能效率·资源利用 | N/A：内网单机无常驻计算，用量不足以度量 | minimal | — | N/A | — | — |

交互能力（25010）：引用 Design-Brief.md「可访问性与响应式」地板（对比度 ≥ 4.5:1、触控目标 ≥ 44px、SCREEN-1～3 必需状态），Spec 明确不要求更高（可访问：不要求）。

## 3. 延迟与重试预算（韧性维必填）

| 层 | 预算(ms) | 超时(ms) | 重试次数 | 退避 | 失败时的 fallback |
|---|---|---|---|---|---|
| 浏览器（点提交 → 收到回执） | 800 | 4500 | 0 | — | 提示「网络不通，草稿已留」，表单不清空，客服可再点一次（幂等键兜重复） |
| API（鉴权 + 派单规则 + 组装响应） | 2200 | 1200 | 0 | — | 回 500 带工单 id，日志留一行，不自己重试 |
| DB（写工单 + 读组长列表） | 2000 | 600 | 1 | 固定 300ms | 两次都失败抛错交 API；连不上时 `/api/health` 转红 |
| 端到端 | 5000 | — | — | — | 5 秒没到组长列表：组长页顶部提示「同步较慢，请刷新确认」 |

各层之和 800 + 2200 + 2000 = 5000ms，正好压在 §2 第 6 维性能的「录单提交到组长列表可见 ≤ 5 秒」上；重试只放 DB 层（1 次 × 600ms 超时 = 1200ms，塞得进本层 2000ms 预算），浏览器与 API 一律写 0——重试逐层相乘，三层各 3 次就是 27 次。

## 4. 隐含合规扫描

| 法规 / 标准 | 触发原因（Spec 哪个词 / 字段 / 人群） | 影响维度 | 对策 | 验证 | 回写 Spec 条目 |
|---|---|---|---|---|---|
| 个人信息保护法（最小收集、访问留痕） | 客户姓名、电话、地址；「客户电话只客服可见」 | 隐私、安全 | 只收三个字段；电话字段默认仅客服；读取留访问日志 | 角色矩阵测试 + 日志抽查 | 非功能与隐含合规「行业隐含合规」行（已有，本表把对策落实） |

扫描过、无命中的词：支付 / 资金、医疗、未成年人、公众服务、电子签名、加密出口、等保——本系统内网内部使用，不涉及。

## 5. 安全威胁表（命中九类触发之一即必填）

触发：项目外文件访问 / 网络或外部 API / Secret / 用户或 AI 生成的 HTML / 命令执行 / 删除·覆盖·发布 / 大文件与媒体解析 / iframe·postMessage·Bridge / 长任务与并发写回

| THR | 资产 | 入口 | 威胁 | 影响 | 缓解 | 验证 | 关联 ID |
|---|---|---|---|---|---|---|---|
| THR-1 | 客户电话 | 手机页 API（网络） | 维修工改工单 id 越权读他人客户电话 | 隐私泄露、个保法 | 按工单归属 + 角色判权，电话字段服务端剔除 | 权限专项测试：他人工单 → 403；本人工单 → 无电话字段 | SCOPE-3 / Q-2 / 模块 auth |
| THR-2 | 服务器磁盘与进程 | 完工照片上传（大文件与媒体解析） | 超大文件或伪装图片拖垮服务 | 可用性 | 类型白名单 jpg/png、单文件 ≤ 10 MB、只存不解析 | 上传 11 MB 与伪装文件 → 拒绝 | SCOPE-3 / 模块 orders |
| THR-3 | 登录凭证 | 登录接口（Secret） | 明文口令或会话不过期 | 冒用客服身份改单 | 口令哈希、会话 12 小时过期、失败 5 次锁 15 分钟 | 登录专项测试 | 非功能「安全」行 / 模块 auth |

## 6. 质量属性场景明细（critical/high 维度逐条展开）

### 场景 R-1：断网重试不重复建单
- 来源：客服小李 · 刺激：提交时手机热点断网，点了两次提交 · 环境：正常工作时段 · 制品：orders 模块
- 响应：第二次提交命中幂等键，返回同一工单 id · 响应度量：重复建单 0 单
- 验证：回归测试「同幂等键重放只建一单」

### 场景 S-1：越权读取客户电话
- 来源：维修工小张 · 刺激：把 URL 里的工单 id 改成别人的 · 环境：已登录 · 制品：auth + orders
- 响应：他人工单返回 403；本人工单响应不含电话字段 · 响应度量：越权成功 0 次
- 验证：权限专项测试（THR-1）

### 场景 P-1：日志不带电话
- 来源：任何请求 · 刺激：记录工单日志 · 环境：生产 · 制品：全部模块
- 响应：日志只写工单 id 与客户名称 · 响应度量：日志含电话 0 行
- 验证：fitness no-pii-in-logs

## 7. 按模块定档表（有 Architecture-Design 时）

| 模块 | security | privacy | reliability | resilience | safety | 其他 | none/minimal 理由 |
|---|---|---|---|---|---|---|---|
| orders | high | high | high | medium | minimal | 可测试性 medium | safety：纯信息系统 |
| dispatch | medium | low | high | medium | minimal | 可修改性 medium | safety 同上；privacy：不碰电话字段 |
| auth | high | high | medium | medium | minimal | — | safety 同上 |
| notify | low | medium | low | low | minimal | — | 提醒丢了可重发，状态在 orders |
| app-desk / app-mobile | medium | high | low | low | minimal | 归一化 low | 页面不落库；电话只在客服页渲染 |
| storage | high | high | high | medium | minimal | 可安装性 medium | safety 同上 |

## 8. AI 产品附加表（Spec 有「AI 能力」段时必填）

Spec 无 AI 能力段，本表不适用。

## 9. 取舍记录（被牺牲方 + 理由 + 越档理由）

- 韧性只到 medium：不做多副本，三人班组接受 30 分钟内人工重启，换成本第 4 位
- 性能不做缓存层：20 人在线 5 秒可见靠数据库索引即可，换可修改性（ADR-2 后果）
- 隐私定 high 而不是 critical：电话字段泄露是合规事故不是人身事故，且无 catalog 时 critical 与 high 执法一样

## 10. 评审评分卡（评审模式填写）

设计模式，未填。

## 11. 待办

- [ ] Q-2 定下后回改 `auth.can` 默认值与本表第 5 行
- [ ] 上线 30 天后回来定监控与告警（Architecture-Design §7 押后）
