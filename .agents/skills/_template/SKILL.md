# <skill-name>

- 版本：0.1.0
- 适用：何时用（一句话）；何时不用（一句话）。
- 输入：需要的上下文（路径/命令/参数）。
- 输出：产出的文件或结论（含路径）。

## 步骤

1. 读 `AGENTS.md`、`ARCHITECTURE.md` 相关条款。
2. （按技能填写具体步骤，保持 ≤ 7 步）
3. 产出结果并给出验证命令。

## 约束与红线

- 遵循 `AGENTS.md` §5（安全与边界）：不碰 `.secrets`/grader 私域，不全盘扫描。
- 遵循 `ARCHITECTURE.md` 红线 2（最小改动）与红线 4（显式降级）。
- 写范围仅限：（填写最小路径白名单）。

## 验收

```bash
# 可执行的验收命令示例（二选一填写真实命令）
bash scripts/smoke.sh
# 期望：退出码 0，输出含 ALL SMOKE CHECKS PASSED
```

## 示例

- 输入示例：（一行）
- 期望输出：（一行）
